import { selectTest } from "../redux/selectors"

// describe("redux selectors", () => {
//   it("should select users from state", () => {
//     const users = [{ id: 1, name: "Redux" }]

//     expect(selectTest({ users })).toEqual(users)
//   })
// })
